import random

class TBus:
    def __init__(self, brand, weight, number_of_places, number_of_passengers):
        self.brand = brand
        self.weight = weight
        self.number_of_places = number_of_places
        self.number_of_passengers = number_of_passengers
    def number_of_passengers_and_weight_of_bus(self):
        if self.number_of_passengers >= 0 and number_of_passengers <= self.number_of_places:
            print('Пасажирів зараз у автобусі={0}'.format(self.number_of_passengers))
            print('Вага автобуса зараз:{0}'.format(self.weight+self.number_of_passengers*weight_of_passengers))
            while True:
                i = random.randint(-self.number_of_passengers, self.number_of_places - self.number_of_passengers)
                try:
                    input()
                    print('Пасажирів зараз у автобусі={0}'.format(self.number_of_passengers))
                    print('Приблизна вага автобусу з пасажирами зараз:{0}'.format(self.weight+self.number_of_passengers*weight_of_passengers))
                except ValueError:
                    print()
                self.number_of_passengers = self.number_of_passengers + i
        else:
            print('Некоректне значення!')

weight_of_passengers = 80  # приблизна вага одного пасажира
number_of_places = int(input('Places='))
number_of_passengers = int(input('Passengers='))
call = TBus('Mercedes', 5000, number_of_places, number_of_passengers)
callfunc = call.number_of_passengers_and_weight_of_bus()
